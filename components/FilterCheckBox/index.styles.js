import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  checkboxContainer: {
    flexDirection: 'row',
    marginBottom: 10,
    alignItems: 'center'
  }
})

export { styles }
