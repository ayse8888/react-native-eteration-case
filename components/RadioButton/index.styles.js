import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    filterBtn: {
        marginBottom: 10,
        borderWidth: 1,
        width: '100%',
        padding: 15,
        borderRadius: 5,
        alignItems: 'center',
        justifyContent: 'center',
    },
})

export { styles }
